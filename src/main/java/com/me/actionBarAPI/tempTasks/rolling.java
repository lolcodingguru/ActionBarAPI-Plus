package com.me.actionBarAPI.tempTasks;

import com.me.actionBarAPI.utils.priorityHandling;
import org.bukkit.Sound;
import org.bukkit.entity.Player;

public class rolling {
    protected boolean rolling(Player player, String message, int duration, priorityHandling.Priority priority, Sound sound){
        return true;
    }
}
